sudo: required
language: node_js

services:
  - docker

before_install:
  - docker build -t ldsimonassi/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true ldsimonassi/docker-react npm run test